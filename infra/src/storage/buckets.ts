/**
 * Storage — S3 Buckets.
 *
 * Vacío a propósito: la base no crea buckets. Cuando un proyecto los necesite,
 * crear aquí los `sst.aws.Bucket` y añadir `import "./storage/buckets"` en app.ts.
 * Ver ROADMAP.md (Fase storage).
 */
export {};
