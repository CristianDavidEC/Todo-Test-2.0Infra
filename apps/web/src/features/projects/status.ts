/** Etiquetas legibles para `Project.status` (BR-7). Mapea el enum del backend al UI. */
export const STATUS_LABELS: Record<string, string> = {
  active: "Activo",
  paused: "En pausa",
  completed: "Completado",
};
